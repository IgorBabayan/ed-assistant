<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:translations="clr-namespace:EdAssistant.Translations"
             xmlns:helpers="clr-namespace:EdAssistant.Helpers"
             xmlns:vm="clr-namespace:EdAssistant.ViewModels.Pages"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="EdAssistant.Views.Pages.SettingsView"
             x:DataType="vm:SettingsViewModel">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Border Margin="5" 
                Grid.Row="0" 
                Grid.Column="0"
                Classes="card">
            <DockPanel>
                <TextBlock Text="{Binding [Settings.DockGroup], Source={x:Static translations:Localization.Instance}}"
                           Classes="title"
                           HorizontalAlignment="Center"
                           DockPanel.Dock="Top"/>
                <WrapPanel Orientation="Vertical"
                           helpers:WrapPanelHelper.SpacingX="12"
                           helpers:WrapPanelHelper.SpacingY="8">
                    <CheckBox Content="{Binding [Settings.Home], Source={x:Static translations:Localization.Instance}}" IsEnabled="False" IsChecked="True"></CheckBox>
                    <CheckBox Content="{Binding [Settings.Cargo], Source={x:Static translations:Localization.Instance}}" IsChecked="{Binding Cargo}"></CheckBox>
                    <CheckBox Content="{Binding [Settings.Materials], Source={x:Static translations:Localization.Instance}}" IsChecked="{Binding Materials}"></CheckBox>
                    <CheckBox Content="{Binding [Settings.Storage], Source={x:Static translations:Localization.Instance}}" IsChecked="{Binding Storage}"></CheckBox>
                    <CheckBox Content="{Binding [Settings.System], Source={x:Static translations:Localization.Instance}}" IsChecked="{Binding System}"></CheckBox>
                    <CheckBox Content="{Binding [Settings.Planet], Source={x:Static translations:Localization.Instance}}" IsChecked="{Binding Planet}"></CheckBox>
                    <CheckBox Content="{Binding [Settings.MarketConnector], Source={x:Static translations:Localization.Instance}}" IsChecked="{Binding MarketConnector}"></CheckBox>
                    <CheckBox Content="{Binding [Settings.Log], Source={x:Static translations:Localization.Instance}}" IsChecked="{Binding Log}"></CheckBox>
                    <CheckBox Content="{Binding [Settings.Settings], Source={x:Static translations:Localization.Instance}}" IsEnabled="False" IsChecked="True"></CheckBox>
                </WrapPanel>
            </DockPanel>
        </Border>

        <Border Margin="5"
                Grid.Row="0" 
                Grid.Column="1"
                Classes="card">
            <DockPanel>
                <TextBlock Text="{Binding [Settings.AppGroup], Source={x:Static translations:Localization.Instance}}"
                           Classes="title"
                           HorizontalAlignment="Center"
                           DockPanel.Dock="Top"/>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal"
                                Grid.Row="0"
                                Margin="0,5,0,0">
                        <TextBox Text="{Binding JournalsFolderPath}"
                                 Watermark="{Binding [Settings.PathToJournals], Source={x:Static translations:Localization.Instance}}"
                                 VerticalAlignment="Stretch"/>
                        <Button Content="..."
                                Command="{Binding BrowseFolderCommand}"
                                Margin="5,0,0,0"
                                VerticalAlignment="Stretch"
                                HorizontalContentAlignment="Center"
                                Width="25"></Button>
					</StackPanel>
					<Button Grid.Row="1"
                            Classes="primary"
                            VerticalAlignment="Bottom"
                            HorizontalAlignment="Right"
                            Command="{Binding ReadAllCommand}"
                            Content="{Binding [Settings.ReadAll], Source={x:Static translations:Localization.Instance}}"></Button>
                </Grid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
